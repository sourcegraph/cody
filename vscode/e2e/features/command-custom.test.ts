import { fixture as test } from '../utils/vscody'

test.use({
    templateWorkspaceDir: 'test/fixtures/workspace',
})

test.describe('custom command', {}, () => {
    // this has entirely changed since the last time this test-suite was created
})
