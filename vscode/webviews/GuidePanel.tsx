import type { VSCodeWrapper } from './utils/VSCodeApi'

export const GuidePanel: React.FunctionComponent<{ vscodeAPI: VSCodeWrapper }> = ({ vscodeAPI }) => {
    return <div>This is a guide panel</div>
}
