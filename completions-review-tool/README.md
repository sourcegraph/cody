# Completions review tool

A tool for reviewing completions suggested by various LLMs.

## Usage

```sh
pnpm run dev
```

## Deployment

Build for production:

```sh
pnpm run build
```

Then run the app in production mode:

```sh
pnpm run start
```
