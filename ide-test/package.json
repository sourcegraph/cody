{
  "name": "ide-test",
  "version": "1.0.0",
  "private": true,
  "description": "A small, heavy, slow test suite",
  "scripts": {
    "test": "playwright install chromium && tsc --build && playwright test"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@playwright/test": "1.47.2",
    "@types/node": "20.12.7"
  },
  "dependencies": {
    "ide-test": "link:"
  }
}
