const debug = new Debug(document.body, {
    dataStyles: {
        top 10px left 10px fixed[[caret]]
        zIndex: '1000',
        color: '#fff',
        backgroundColor: 'var(--color-overlay)',
        padding: '8px',
        fontSize: '12px',
        border: '2px solid var(--color-text-primary)',
    },
});
